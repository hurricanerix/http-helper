/*
Package python implements handlers and middleware useful for responding
as a drop in replacement for the command `python -m http.server`.  The
primary focus being on matching request and responses and secondarily
matching the server side logging when convenient.
*/
package python
